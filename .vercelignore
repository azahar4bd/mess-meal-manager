# Local-only files that must not be uploaded to Vercel.
# NOTE: drizzle/ (including drizzle/meta/_journal.json) MUST be uploaded —
# POST /api/migrations reads it at runtime to create the schema.
.deploy/
.env.local
.env*.local
*.dump
scripts/
docs/
google-apps-script/
